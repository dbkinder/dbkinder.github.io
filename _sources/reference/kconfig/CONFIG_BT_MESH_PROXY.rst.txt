:orphan:

.. title:: BT_MESH_PROXY

.. option:: CONFIG_BT_MESH_PROXY
.. _CONFIG_BT_MESH_PROXY:

The configuration item CONFIG_BT_MESH_PROXY:

:Symbol:           BT_MESH_PROXY
:Type:             bool
:Value:            "n"
:User value:       (no user value)
:Visibility:       "n"
:Is choice item:   false
:Is defined:       true
:Is from env.:     false
:Is special:       false
:Prompts:
 (no prompts)
:Default values:
 (no default values)
:Selects:
 (no selects)
:Reverse (select-related) dependencies:
 BT_HCI && BT && BT_HCI_HOST && BT_MESH && BT_CONN && BT_MESH_PB_GATT || BT_HCI && BT && BT_HCI_HOST && BT_MESH && BT_CONN && BT_MESH_GATT_PROXY (value: "n")
:Additional dependencies from enclosing menus and ifs:
 BT_HCI && BT && BT_HCI_HOST && BT_MESH && BT_CONN (value: "n")
:Locations:
 * ../subsys/bluetooth/host/mesh/Kconfig:36
